# سند برنامه‌ریزی و نقشه راه توسعه سیستم ورود خودکار به پورتال و مدیریت دروس پاس‌شده (UniSchedule)

## ۱. مرور معماری پروژه و هدف تغییرات

پروژه فعلی یک اپلیکیشن **Electron + React + TypeScript + Vite** است که برای انتخاب واحد و زمان‌بندی دروس دانشگاه صنعتی امیرکبیر (و سایر دانشگاه‌ها) استفاده می‌شود.

### چالش فعلی:
در حال حاضر کاربر مجبور است HTML گزارش ۲۱۲ بهستان را دستی کپی کرده و در برنامه پیست کند.

### هدف اصلی:
1. **دریافت خودکار دروس (ویژه ویندوز و لینوکس)**: افزودن دکمه «ورود به سایت» جهت باز شدن مرورگر داخلی در محیط ویندوز و لینوکس (به صورت چندسکویی/Cross-Platform در Electron)، لاگین در پورتال امیرکبیر (`portal.aut.ac.ir`) از طریق سامانه یکپارچه (`accounts.aut.ac.ir/cas/login`)، جستجوی گزارش ۲۱۲، کلیک روی «نمایش جدولی» و استخراج مستقیم کد HTML از پنجره جدید بدون دست زدن به منطق پارسر موجود (`parseAutBehestan`).
2. **قابلیت ثبت دروس پاس‌شده (Passed Courses)**: هدایت کاربر به بخش «اطلاعات تحصیلی»، استخراج دروس تمام ترم‌های طی‌شده هنگام ورق زدن ترم‌ها، و فیلتر/مخفی‌سازی خودکار این دروس از بانک دروس هنگام انتخاب واحد.
3. **حفظ کامل منطق قبلی برنامه**: عدم دستکاری در کدهای قبلی پارسر و ذخیره‌سازی، و صرفاً استفاده مجدد و توسعه الحاقی روی کدهای موجود.
4. **پشتیبانی کامل و یکپارچه در لینوکس و ویندوز**: بهره‌گیری از قابلیت نیتیو `BrowserWindow` در Electron که در هر دو سیستم‌عامل ویندوز و لینوکس به طور کامل و یکسان پشتیبانی می‌شود.

---

## ۲. ریز کارهای اجرایی (Task Checklist)

### فاز ۱: توسعه مرورگر داخلی در Electron (چندسکویی: ویندوز + لینوکس)

- [x] **۱.۱. افزودن IPC Handlers اختصاصی در `electron/main.cjs`**
  - ایجاد handler جدید `open-aut-portal-window` برای باز کردن پنجره اختصاصی مرورگر Electron (`BrowserWindow`).
  - تنظیم ابعاد متناسب (1100x750) و تنظیم Preload Script مجهز به IPC.
  - تنظیم آدرس اولیه پنجره روی `https://portal.aut.ac.ir/browser/fa/#/auth/login`.
  - سازگاری کامل اجرای پنجره در ویندوز و توزیع‌های مختلف لینوکس (محیط‌های X11 و Wayland).

- [x] **۱.۲. شنود و مدیریت پنجره‌های جدید (Popup Handling)**
  - تنظیم `setWindowOpenHandler` در پنجره پورتال تا وقتی کاربر روی دکمه «نمایش جدولی» کلیک می‌کند (که پنجره جدید باز می‌کند)، پنجره پاپ‌آپ مسدود نشود.
  - تزریق اسکریپت شنود در پنجره پاپ‌آپ جدید و ارسال محتوای DOM آن به پنجره اصلی اپلیکیشن پس از بارگذاری کامل جدول.

- [x] **۱.۳. استخراج خودکار HTML گزارش ۲۱۲ و ارسال به پنجره اصلی**
  - شناسایی جدول دروس در پنجره پاپ‌آپ (بر اساس وجود تگ `table` و ستون‌های مربوط به لیست دروس).
  - خواندن `document.documentElement.outerHTML` پنجره پاپ‌آپ و ارسال آن از طریق کانال IPC با نام `aut-courses-html-captured`.
  - بسته شدن خودکار یا دستی پنجره پورتال پس از استخراج موفق و نمایش پیام موفقیت به کاربر.

- [x] **۱.۴. بروزرسانی UI در `src/components/CourseCatalogModal.tsx`**
  - افزودن دکمه «ورود به سایت و دریافت خودکار دروس (ویندوز / لینوکس)» با آیکون مرورگر/قفل در کارت اختصاصی روش هوشمند خودکار.
  - شنود پیام IPC دریافت HTML در کامپوننت و فراخوانی مستقیم تابع موجود `parseAutBehestan(capturedHtml)`.
  - ذخیره‌سازی دروس در IndexedDB/localStorage با توابع موجود `saveFreshCatalogSource` یا `appendCatalogSource`.

---

### فاز ۲: قابلیت استخراج و مدیریت دروس پاس‌شده (Passed Courses)

- [x] **۲.۱. تعریف ساختار داده و Type اختصاصی**
  - تعریف `PassedCourse` در `src/types/schedule.ts`:
    ```typescript
    export interface PassedCourse {
      code: string;           // کد درس (مثلاً 3108053)
      name: string;           // نام درس (مثلاً مبانی هوش محاسباتی)
      credits?: number;       // تعداد واحد (مثلاً 3)
      termName?: string;      // ترم گذرانده شده (مثلاً ترم اول 05-04)
      group?: string;         // شماره گروه (مثلاً 01)
      courseType?: string;    // نوع درس (تخصصی/عمومی/پایه)
      status?: string;        // وضعیت (ثبت نهایی / پاس شده)
      grade?: string;         // نمره (در صورت وجود)
      passedAt: number;       // زمان ثبت
    }
    ```

- [x] **۲.۲. پیاده‌سازی پارسر اختصاصی دروس پاس‌شده (`src/utils/parsers/autPassedCoursesParser.ts`)**
  - تحلیل متنی و HTML صفحه «اطلاعات تحصیلی» پورتال بهستان مطابق نمونه متنی داده‌شده:
    - استخراج اطلاعات دانشجو (نام و نام خانوادگی، شماره دانشجویی، معدل کل).
    - شناسایی بلاک‌های ترم‌ها (مثلاً `ترم اول 06-05`, `ترم دوم 05-04`).
    - استخراج کد درس، نام درس، تعداد واحد و وضعیت ثبت‌نام/قبولی (نظیر «ثبت نهایی»، نمره یا گذرانده‌شده).
    - نرمال‌سازی کدهای درسی (حذف شماره گروه جهت تطبیق پایه کد درس).

- [x] **۲.۳. طراحی مدال/راهنمای تعاملی ذخیره دروس پاس‌شده**
  - ارسال محتوای DOM صفحه اطلاعات تحصیلی با هر تغییر/ورق زدن ترم توسط کاربر از طریق IPC کانال `aut-passed-courses-html-captured`.
  - به‌روزرسانی و متقاطع‌سازی (Merge & Deduplicate) لیست دروس پاس‌شده بر اساس `code` درس.

- [x] **۲.۴. ذخیره‌سازی و مدیریت دروس پاس‌شده در Client Storage**
  - ایجاد ماژول مدیریت دروس پاس‌شده در `src/utils/passedCoursesStorage.ts`:
    - `savePassedCourses(courses: PassedCourse[])`
    - `getPassedCourses(): PassedCourse[]`
    - `appendPassedCourses(newCourses: PassedCourse[])`
    - `clearPassedCourses()`
    - `isCoursePassed(passedCourses, code, name)`

- [x] **۲.۵. فیلتر خودکار دروس پاس‌شده در بانک دروس (`CourseCatalogModal.tsx`)**
  - هنگام نمایش دروس بانک دروس یا اضافه کردن درس به برنامه زمان‌بندی:
    - بررسی مطابقت کد درس انتخاب‌شده یا نام درس با لیست `PassedCourse`.
    - افزودن گزینه فیلتر: «پنهان‌سازی دروس پاس‌شده» (پیش‌فرض: فعال).

---

### فاز ۳: یکپارچه‌سازی با منطق موجود (No Breaking Changes)

- [x] **۳.۱. حفظ کامل توابع و ساختار پارسر قبلی (`autParser.ts`)**
  - عدم دستکاری تابع `parseAutBehestan`. داده استخراج‌شده از پنجره پاپ‌آپ دقیقا همان جدول HTML استاندارد گزارش ۲۱۲ است که پارسر قبلی بر پایه آن نوشته شده است.
- [x] **۳.۲. حفظ مکانیزم ذخیره‌سازی کاتالوگ (`catalogStorage.ts`)**
  - استفاده مستقیم از توابع `saveFreshCatalogSource` و `appendCatalogSource` جهت ثبت داده‌های دریافت شده از پورتال.
- [x] **۳.۳. حفظ سازگاری نسخه وب (Web Fallback)**
  - در صورتی که برنامه در مرورگر عادی (غیر Electron) اجرا شود، دکمه ورود به سایت پیغام راهنما مبنی بر «این قابلیت فقط در نسخه دسکتاپ ویندوز/لینوکس فعال است» نمایش دهد و حالت کپی-پیست دستی HTML همچنان فعال بماند.

---

### فاز ۴: تست و صحت‌سنجی (Verification & Testing)

- [x] **۴.۱. تست چرخه ورود و استخراج لیست دروس**
  - لاگین در سامانه یکپارچه -> باز کردن گزارش ۲۱۲ -> کلیک روی «نمایش جدولی» -> استخراج خودکار و اضافه شدن دروس به کاتالوگ.
- [x] **۴.۲. تست چرخه استخراج دروس پاس‌شده**
  - باز کردن اطلاعات تحصیلی -> ورق زدن ۳ ترم متوالی -> استخراج کامل دروس -> مشاهده لیست دروس پاس‌شده و عدم امکان اخذ مجدد آنها.
- [x] **۴.۳. تست تداخل‌ها و تکراری‌ها**
  - اطمینان از اینکه دروس تکراری در بانک دروس ذخیره نمی‌شوند و لایه‌های موجود بررسی تداخل زمان کلاس/امتحان بدون تغییر کار می‌کنند.

---

## ۳. راهنمای کامل راه‌اندازی و توسعه نسخه لینوکس (Linux Implementation Guide)

برنامه **UniSchedule** بر پایه Electron توسعه یافته است، بنابراین قابلیت اجرای نیتیو (Native) روی تمامی توزیع‌های لینوکس (Debian/Ubuntu, Fedora, Arch Linux) را دارا می‌باشد و مرورگر داخلی ایجادشده با `BrowserWindow` به صورت صد درصد هم روی ویندوز و هم روی لینوکس کار می‌کند.

### ۳.۱. نصب نیازمندی‌ها و وابستگی‌های سیستمی در لینوکس

برای اجرای Electron و مرورگر داخلی Chromium در لینوکس، کتابخانه‌های زیر روی سیستم‌عامل باید نصب باشند:

#### در توزیع‌های مبتنی بر اوبونتو / دبیان (Ubuntu / Debian):
```bash
sudo apt update
sudo apt install -y build-essential libnss3 libatk1.0-0 libatk-bridge2.0-0 \
    libcups2 libdrm2 libgdk-pixbuf2.0-0 libgtk-3-0 libgbm1 libasound2 \
    libxcomposite1 libxdamage1 libxfixer3 libxrandr2 libxtst6 xauth xvfb
```

#### در توزیع‌های مبتنی بر فدورا (Fedora / RHEL):
```bash
sudo dnf install -y nss atk at-spi2-atk cups-libs libdrm gdk-pixbuf2 \
    gtk3 mesa-libGBM alsa-lib libXcomposite libXdamage libXfixes libXrandr libXtst
```

#### در توزیع‌های مبتنی بر آرچ (Arch Linux / Manjaro):
```bash
sudo pacman -S --needed nss atk at-spi2-atk cups gdk-pixbuf2 gtk3 \
    mesa alsa-lib libxcomposite libxdamage libxfixes libxrandr libxtst
```

---

### ۳.۲. تنظیم اسکریپت‌های Build لینوکس در `package.json`

جهت خروجی گرفتن نسخه لینوکس (فرمت‌های `.AppImage` و `.deb`)، بخش `build` در فایل `package.json` را به صورت زیر تکمیل کنید:

```json
"build": {
  "appId": "com.unischedule.app",
  "productName": "UniSchedule",
  "linux": {
    "target": [
      "AppImage",
      "deb"
    ],
    "category": "Education",
    "icon": "build/icon.png"
  }
}
```

و اسکریپت‌های اجرایی در `package.json`:
```json
"scripts": {
  "electron:build:linux": "vite build --base=./ && electron-builder --linux"
}
```

---

### ۳.۳. نکات اختصاصی اجرا و مرورگر داخلی Electron در لینوکس

1. **سازگاری کامل BrowserWindow در لینوکس**:
   - پنجره مرورگر داخلی ایجادشده در Electron (`BrowserWindow`) در لینوکس دقیقا مانند ویندوز به‌صورت نیتیو با کرومیوم اجرا می‌شود و کاملا با سامانه یکپارچه امیرکبیر (`portal.aut.ac.ir`) سازگار است.
2. **پشتیبانی از Wayland و X11**:
   - در صورت استفاده از Wayland در توزیع‌های جدید لینوکس، Electron به طور خودکار یا با سوییچ `--enable-features=UseOzonePlatform --ozone-platform=wayland` اجرا می‌شود.
3. **اجرا در محیط Sandbox لینوکس**:
   - در صورتی که برنامه درون Docker، container یا برخی توزیع‌های لینوکس با دسترسی محدود اجرا شود، در فایل `electron/main.cjs` هنگام ایجاد `BrowserWindow` می‌توانید پارامترهای زیر را بفرستید:
     ```js
     app.commandLine.appendSwitch('no-sandbox');
     ```

---

## ۴. جمع‌بندی

تمامی فازهای ۱، ۲ و ۳ با موفقیت پیاده‌سازی شدند:
1. قابلیت ورود به پورتال و مرورگر داخلی چندسکویی (ویندوز + لینوکس).
2. استخراج خودکار جدول دروس گزارش ۲۱۲ و فراخوانی پارسر بدون تغییر در منطق قبلی.
3. استخراج دروس پاس‌شده از اطلاعات تحصیلی و فیلتر خودکار دروس پاس‌شده.
